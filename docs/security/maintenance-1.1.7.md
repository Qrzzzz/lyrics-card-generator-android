# 1.1.7 runtime and Renderer maintenance

Scope: plan Step 7–9, baseline main `03dd96b934683455b99f27ee163eced59ad74165`, public v1.1.6. Baseline Quality Gate 35049078866 and Dependency Security 35049078872 both completed successfully. Live baseline: only issue #45 and React PR #56 open; zero open Dependabot alerts. Prior failures and original advisory deadlines remain in historical records.

## Decisions

- [Coroutines 1.11.0](https://github.com/Kotlin/kotlinx.coroutines/releases/tag/1.11.0) replaces 1.9.0, including cancellation/SharedFlow and R8 stateIn fixes. Android and test artifacts share the catalog version.
- [Serialization 1.11.0](https://github.com/Kotlin/kotlinx.serialization/releases/tag/v1.11.0) replaces 1.7.3. Kotlin 2.4.20 remains; the newer serialization 1.12.0-RC is a prerelease and is not needed. Persisted schema 1 and `android-alpha-renderer-1` remain unchanged.
- #56 was updated to current main before review. Its four React/runtime/types 19.3 updates are included equivalently here, preserving serial human PR integration.
- React 19.3 passed typecheck/tests/build before Vite migration. Vite 8.3.0 and plugin-react 6.1.1 satisfy the plugin's Vite ^8 peer and Node24 engines; that pair passed the same checks before TypeScript 7.0.2 was installed and checked independently. No force/legacy-peer-deps. Explicit chrome69 build target, local base URL, schema/font emission and pixel references remain intact. See [Vite migration](https://vite.dev/guide/migration).
- AGP 8.13.2 / Gradle 8.14.4 / JDK17 hosted builds / KSP 2.3.12 / Kotlin 2.4.20 / R8 9.1.29 and the host-only security constraints stay in place. AGP9 and unrelated major migrations offer no needed repair here. Actions and AndroidTest backlog decisions/evidence remain in the 1.1.5 and 1.1.6 reports. Optional extended device matrices are on-demand diagnostics, not an indefinitely deferred release prerequisite.

## Regression coverage and evidence

Implementation `06da61a56857490db173605ac4aaabb60528c9e7`:

- PASS: separate React, Vite/plugin, and TypeScript `npm run check`; final clean `npm ci --no-audit --no-fund`.
- PASS: `npm run golden:test`, all 30 existing PNG references exact, including 1×/2× and fresh-page serif ordering. References were not regenerated.
- PASS: `npm run audit:security`, zero findings including development dependencies.
- Added queued-export cancellation coverage: cancelling a mutex waiter cannot cancel the active export or prevent another export. Existing tests cover active cancellation, timeout, stale callbacks, temporary-file cleanup, delayed save outcomes and finalization.
- Added sparse legacy project edit/save/reopen, invalid JSON/type/enum rejection, and Native/Renderer envelope roundtrip/unknown fields/missing required fields. Existing repository tests retain corrupt rows and preserve concurrent export metadata.
- Local first invocation was NOT RUN due to an invalid debug AndroidTest task name; corrected to this repository's release test variant. This tooling error is not a product failure.
- PASS: local release JVM 165/165; `:app:testProductionReleaseUnitTest :app:assembleProductionRelease :app:assembleProductionReleaseAndroidTest --no-parallel --no-configuration-cache --console=plain`, 92 tasks executed, 2m34s, exit 0. Local JBR21/JVM17 target; hosted JDK17 remains independently checked.
- PASS: `:app:dependencies --configuration productionReleaseRuntimeClasspath` resolves coroutines Android/core and serialization JSON/core to 1.11.0, including alignment of older transitive requests. Reproducible commands and local counts are recorded in [local regression evidence](../releases/v1.1.7-local-regression.json).
- PASS: fresh-install API30 AVD `lcg_113_api30_probe`, WebView 83.0.4103.106, locally debug-signed release APK SHA-256 `a9a91d2f10902d13608bf97fe45b55e93a0b3679ce6d8f33112ee32b77fb3837`. Actual instrumentation: serif 1x/2x, cancellation/partial cleanup/retry, six-step UI/preview/exports (3 tests, 57.731s); system save/share-sheet and exported-byte integrity (1 test, 30.404s). Tests use the embedded built Renderer. No image was sent to a third-party contact. This is local emulator evidence, not a production-candidate installation or physical-device observation.
- PASS: [PR #69 Quality Gate 35049965700](https://github.com/Qrzzzz/lyrics-card-generator-android/actions/runs/35049965700), head `56bedb8404265e3a8a4483078bfc2f0bb20dbd69`: full Android, Unicode JVM, Renderer, contracts and audit, plus [Dependency Review 35049965759](https://github.com/Qrzzzz/lyrics-card-generator-android/actions/runs/35049965759). Merged as `65619c9ea12083e748509b62cc4fea40edf956fd`; #56 is equivalently superseded and closed.
- PASS: same-source [main Quality Gate 35050868170](https://github.com/Qrzzzz/lyrics-card-generator-android/actions/runs/35050868170) and [Dependency Security 35050868195](https://github.com/Qrzzzz/lyrics-card-generator-android/actions/runs/35050868195), source `65619c9ea12083e748509b62cc4fea40edf956fd`. Default-branch submission completed and the live API still reports zero open alerts. PR Android logs independently confirm four actual JVM variants and 244 executed Gradle tasks, including security resolution, release lint/R8, APK/AAB, bundletool manifest reading and release test APK packaging.
- PASS: [Production candidate 35051691716 / attempt 1](https://github.com/Qrzzzz/lyrics-card-generator-android/actions/runs/35051691716), frozen source `65619c9`, version 1.1.7/10107, normal production-signing environment approval. Production certificate remains `b02a1b6c391545c2bdbcc33bda3a708741e259a4efea60bdc4958522f5bc82f5`. [Attestation 47810637](https://github.com/Qrzzzz/lyrics-card-generator-android/attestations/47810637) covers the five public assets; test APK has separate attestation 47810643.
- PASS: original artifact 10429646596 (`production-candidate-1.1.7-65619c9ea120`, 118280699 bytes) downloaded once; archive digest `fe73934e69216eb63344a7daec6fd99e8e89fffca8854d9f8e3803498bdaf726` matches the API. `validateAcceptance` and `validateFiles` passed on all five assets, metadata/source/run/attempt/version/certificate and checksums. An initial artifact-list API EOF was retried before the successful transfer; no rebuild/re-sign occurred.
- The [version-bound publication authorization](../releases/v1.1.7-acceptance.json) binds APK SHA-256 `19e30192454ff93d221be9e8d2a305d0ec41fd978deb3d37cbbd384d7d25847b`. Six physical manual checks remain NOT RUN with device=null. Original attested metadata remains PROVISIONAL/NOT RUN/finalReady=false; final publication verifies all five attestations and remote digests without rewriting it.
- NOT RUN: physical-device six manual checks, complete multi-API matrix, 20 exports, 30-minute endurance, low-memory, TalkBack and large-font tests. Local emulator evidence does not replace physical-device observations or signed-candidate provenance.

Issue #45 closes only after integration and public release verification. New ordinary Dependabot updates after this baseline belong to the normal monthly maintenance cycle.

## Branch disposition

The two completed local 1.1.5 branches matched the entire trees of their merged PR #63/#65 commits. Both refs were saved in `C:\Users\qrzzz\Downloads\lcg117-completed-branches.bundle`, verified with `git bundle verify` and exact ref/SHA listing, before deletion. Superseded React #56's updated head `9a9975506e91191da17ae4a0416a8bd834185301` was separately backed up and verified in `lcg117-react-superseded.bundle` before remote deletion with an exact-head lease.

Five historical local branches still have non-equivalent/unknown commits: `codex/fix-test-apk-inspection`, `codex/issue9-real-talkback`, and `codex/rescue-20260810-{489c,4a6e,f6ed}`. They remain preserved; they are not active remote maintenance PRs and are not falsely reported as safely merged/deleted. The unrelated `lcg-115-validation` and `lcg-112-bc-signing-*` worktrees remain untouched.
