# v2.3 — 稳定图片加载与最终布局

版本 `2.3 / 20300`，以 2.2 为基础修复 #78，并整体回归 #73–#78。保留包名、生产签名、2.x 项目兼容与 `android-alpha-renderer-1` 合同；1.x 项目继续保留但不迁移。

- 依赖封面真实宽高的测量等待 load/error，已缓存图片检查 complete 与自然尺寸；等待上限 4 秒，与字体等待并行。旧 WebView 不需要 decode API，导出末端同样提供加载事件回退。
- 失败或超时后采用稳定占位图，迟到加载不会改变已经确认的布局；重新选择封面可重试。封面组件按资源身份隔离，等待结束清理监听器与定时器，旧资源事件不能覆盖替换后的封面。
- 测量缓存校验资源状态，重新测量清空 SVG 缓存。2.2 完整宽高状态继续贯通预览、尺寸提示、内存估算、导出与原生文件校验。
- 横图真实比例可能使短歌词自然高度小于 640；在布局计划内部应用现有 Android 640px 下限，使所有坐标和最终画布一致，不放宽校验或改变手动高度范围。

## 六单整体回归

| 状态 | 问题 | 自动化覆盖 |
| --- | --- | --- |
| PASS | #73 封面历史保留 | EditorDataLifecycleTest：真实文件、撤销重做、历史淘汰、共享项目引用 |
| PASS | #74 短横版导出 | RendererControllerRecoveryTest 与浏览器：短画布三格式三倍率、解码尺寸和原生文件校验 |
| PASS | #75 完整尺寸状态 | EditorViewModelTest、ExportViewModelTest、RendererControllerRecoveryTest：旧响应拒绝、确认结果复用、实际文件尺寸优先 |
| PASS | #76 导入期间新编辑 | EditorDataLifecycleTest：慢保存失败、后续编辑保留、连续导入互斥 |
| PASS | #77 高度模式转换 | EditorViewModelTest 与浏览器：640/720/3200/4000、保存重开与模式往返 |
| PASS | #78 图片就绪 | images.test.ts 与 test-image-readiness.mjs：600ms 方/横/竖图、404、4 秒超时、快速替换、迟到事件、无 decode API、预览与解码尺寸一致 |

命令与最终源码证据在发布前补齐。浏览器受控图片来自测试路由，生产本地资源与网络隔离规则保持不变。

实体设备六项人工操作、跨 API/WebView 矩阵、20 次导出、低内存、30 分钟耐久、TalkBack、大字体专项均 NOT RUN；自动化结果不替代实体证据。沿用 focused-manual-v1：同 SHA 主干全量 CI、可信生产候选、签名/证书/来源/哈希/attestation 与版本绑定授权后公开原始五附件。发布完成仅核对远端 API/digest，不重新下载附件。